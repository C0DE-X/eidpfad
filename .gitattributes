# Normalize source files on every platform.
* text=auto eol=lf
*.bat text eol=crlf
*.ps1 text eol=crlf

# Runtime binaries are generated from the checked-in source pipelines and are
# intentionally not stored in Git or Git LFS.
