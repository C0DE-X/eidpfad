name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  repository:
    name: Repository hygiene
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Check repository contents
        run: python3 scripts/check_repository.py

  validate:
    name: Rules and asset references
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: server/pyproject.toml

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: npm

      - name: Install server test dependencies
        run: python -m pip install -e './server[dev]'

      - name: Generate runtime assets
        run: make content PYTHON=python

      - name: Reject generated source drift
        run: git diff --exit-code -- shared client/assets/asset_manifest.json client/assets/cards client/assets/items client/assets/enemies

      - name: Run server tests and content validation
        run: make validate PYTHON=python

  godot-import:
    name: Godot import and GDScript parse
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.13"

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: npm

      - name: Install server smoke-test dependencies
        run: python -m pip install -e './server[dev]'

      - name: Generate runtime assets
        run: make content PYTHON=python

      - name: Download verified Godot editor
        shell: bash
        run: |
          set -euo pipefail
          version="$(tr -d '\r\n' < .godot-version)"
          archive="Godot_v${version}-stable_linux.x86_64.zip"
          release_url="https://github.com/godotengine/godot-builds/releases/download/${version}-stable"
          curl --fail --location --retry 3 --output "${archive}" "${release_url}/${archive}"
          curl --fail --location --retry 3 --output SHA512-SUMS.txt "${release_url}/SHA512-SUMS.txt"
          grep -F "${archive}" SHA512-SUMS.txt > SHA512-CHECK.txt
          sha512sum --check SHA512-CHECK.txt
          unzip -q "${archive}" -d .godot-ci
          chmod +x ".godot-ci/Godot_v${version}-stable_linux.x86_64"

      - name: Import project and reject parser errors
        shell: bash
        run: |
          set -euo pipefail
          version="$(tr -d '\r\n' < .godot-version)"
          godot=".godot-ci/Godot_v${version}-stable_linux.x86_64"
          timeout 20m "${godot}" --headless --path client --import 2>&1 | tee godot-import.log
          if grep -E "SCRIPT ERROR|Parse Error|Failed to load script|INTEGER_DIVISION|SHADOWED_GLOBAL_IDENTIFIER|CONFUSABLE_LOCAL_DECLARATION" godot-import.log; then
            echo "Godot reported a GDScript parser, load error or rejected warning."
            exit 1
          fi

      - name: Start and continue a real Singleplayer campaign
        shell: bash
        run: |
          set -euo pipefail
          version="$(tr -d '\r\n' < .godot-version)"
          godot=".godot-ci/Godot_v${version}-stable_linux.x86_64"
          python scripts/run_client_menu_smoke.py --godot "${godot}"

  docker-build:
    name: Server container
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Validate Compose configuration
        env:
          POSTGRES_PASSWORD: ci-only-password
        run: docker compose config --quiet

      - name: Build production server image
        run: docker build --file server/Dockerfile --tag eidpfad-server:ci .
